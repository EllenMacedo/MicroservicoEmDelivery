package com.mycompany.delivery.microservicopagamento2;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.mycompany.delivery")
public class Microservicopagamento2Application {

    public static void main(String[] args) {
        SpringApplication.run(Microservicopagamento2Application.class, args);
    }
}